# מדריך פרסום — מערכת RAG על Streamlit Cloud

## שלב 1 — התקנת חבילות Python (פעם אחת)

פתח **Command Prompt** (cmd) בתיקיית הפרויקט:

```cmd
pip install -r requirements.txt
```

> ⏳ ייקח כ-3–7 דקות בפעם הראשונה (מוריד מודל AI קטן)

---

## שלב 2 — הגדרת מפתח ה-API (מקומית)

צור קובץ `.env` בתיקיית הפרויקט:

```
ANTHROPIC_API_KEY=sk-ant-api03-ה-מפתח-שלך
```

קבל מפתח חינמי בכתובת: https://console.anthropic.com/

---

## שלב 3 — בניית בסיס הידע מהמסמכים

```cmd
python build_db.py
```

הסקריפט יעבד את 9 קבצי ה-PDF ויצור תיקיית `rag_db/`.

> ⏳ ייקח 2–5 דקות (תלוי בגודל הקבצים)

**פלט צפוי:**
```
[1/5] בדיקת תיקיית המסמכים...
  ✅ נמצאו 9 קבצים
[2/5] טוען מסגרת ה-RAG...
[3/5] טוען ומחלק מסמכים לחתיכות...
[4/5] בונה מסד נתוני וקטורים (ChromaDB)...
[5/5] סיכום
  ✅ הבנייה הושלמה בהצלחה!
```

---

## שלב 4 — בדיקה מקומית

```cmd
streamlit run streamlit_app.py
```

הדפדפן ייפתח אוטומטית בכתובת http://localhost:8501

בדוק שהמערכת עובדת — שאל שאלה כמו "מהם תנאי הפרישה?"

---

## שלב 5 — יצירת repository ב-GitHub

1. היכנס ל-[github.com](https://github.com) → לחץ **New repository**
2. שם: `hr-rag-system` (או כל שם שתרצה)
3. **פרטי (Private)** ← חשוב! המסמכים רגישים
4. לחץ **Create repository**

---

## שלב 6 — העלאת הקוד ל-GitHub

פתח cmd בתיקיית הפרויקט:

```cmd
git init
git add streamlit_app.py rag_system.py build_db.py requirements.txt .gitignore
git add .streamlit/
git add rag_db/
git commit -m "Initial RAG system with pre-built knowledge base"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/hr-rag-system.git
git push -u origin main
```

> 🔒 הקבצים בגיט: **כן** — rag_db/, קוד Python, הגדרות
> 🔒 **לא** — קבצי PDF, קובץ .env (בזכות .gitignore)

---

## שלב 7 — פרסום ב-Streamlit Cloud

1. היכנס ל-[share.streamlit.io](https://share.streamlit.io)
2. לחץ **New app**
3. בחר את ה-repository שיצרת
4. **Main file path:** `streamlit_app.py`
5. לחץ על **Advanced settings**

### הגדרת Secrets (מאוד חשוב!)

בשדה **Secrets**, הכנס:

```toml
ANTHROPIC_API_KEY = "sk-ant-api03-ה-מפתח-שלך"
```

6. לחץ **Deploy!**

> ⏳ הפרסום ייקח 2–4 דקות (Streamlit Cloud מתקין חבילות)

---

## שלב 8 — האתר חי! 🎉

תקבל כתובת בפורמט:
```
https://YOUR_USERNAME-hr-rag-system-streamlit-app-XXXXX.streamlit.app
```

---

## עדכון המסמכים בעתיד

אם תוסיף מסמכים חדשים:

```cmd
# 1. הוסף PDF לתיקייה
# 2. הרץ מחדש
python build_db.py

# 3. עדכן GitHub
git add rag_db/
git commit -m "Update knowledge base with new documents"
git push
```

Streamlit Cloud יתרענן אוטומטית.

---

## פתרון בעיות נפוצות

| בעיה | פתרון |
|------|--------|
| `ModuleNotFoundError` | הרץ `pip install -r requirements.txt` |
| `ANTHROPIC_API_KEY not found` | בדוק קובץ `.env` או Secrets ב-Streamlit Cloud |
| `rag_db/ empty` | הרץ `python build_db.py` תחילה |
| אתר אינו בעברית | רענן עמוד (Ctrl+F5) |
| תשובות איטיות | נורמלי בשלוש הבקשות הראשונות — המודל מתחמם |

---

## מבנה הפרויקט הסופי

```
hr-rag-system/
├── streamlit_app.py        ← ממשק המשתמש (Streamlit)
├── rag_system.py           ← ליבת ה-RAG
├── build_db.py             ← סקריפט בניית מסד הנתונים
├── requirements.txt        ← חבילות Python
├── .gitignore              ← מה לא להעלות ל-GitHub
├── .streamlit/
│   └── config.toml         ← הגדרות עיצוב
└── rag_db/                 ← ChromaDB (נוצר ע"י build_db.py)
    ├── chroma.sqlite3
    └── ...
```
